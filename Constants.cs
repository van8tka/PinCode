namespace PinCode
{
    public class Constants
    {
        public enum PinCodeMode
        {
            CREATE,
            CHANGE,
            CHECK
        }

        public const string EXTRA_MODE = "mode";
    }
}